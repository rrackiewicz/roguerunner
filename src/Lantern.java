/**
 * The Lantern enum represents all the modes of lanterns
 */
public enum Lantern {	
	FULL, //can appear invisible for a short time
	FOW,  //implements a fog of war via a spirit
	CONE  //lantern only illuminates a specific radius
}